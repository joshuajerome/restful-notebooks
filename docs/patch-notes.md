# Patch Notes

## v0.1.0 (2026-03-28)

### Features

- **cap030** — Initial scaffold: FastAPI backend wrapping restful library, React + MUI frontend with request builder, endpoint browser, workflow editor, workspace configuration, request history, audit log. Electron shell with splash screen and backend lifecycle management.
