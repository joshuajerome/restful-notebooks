[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "post-it-desktop-backend"
version = "0.1.0"
description = "post-it desktop backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "sqlalchemy>=2.0.0",
    "aiosqlite>=0.20.0",
    "pydantic>=2.0.0",
    "post-it",
]

[tool.uv.sources]
post-it = { path = "../../post-it", editable = true }

[dependency-groups]
dev = [
    "ruff>=0.11.0",
    "ty>=0.0.1a0",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]

[tool.setuptools]
package-dir = { "" = "." }
